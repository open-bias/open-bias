# NeMo Guardrails evaluator config — integrates NVIDIA NeMo rails.

port: 4000

evaluators:
  - name: nemo-rails
    type: nemo
    rules_file: ./rules.md
    # fail_closed: false      # uncomment to deny on NeMo errors instead of warn
