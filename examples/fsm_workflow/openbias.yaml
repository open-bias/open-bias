# FSM evaluator config — deterministic workflow enforcement.
#
# The FSM engine uses embedding-based semantic classification to match
# conversation turns to workflow states. If omitted, the classifier model
# is auto-detected from API keys.

port: 4000

evaluators:
  - name: customer-support
    type: fsm
    rules_file: ./rules.md
