# Minimal config — just an evaluator. Rules live in project rules.md.
# Everything else has
# smart defaults.
#
# Open Bias auto-detects:
#   - Model: first available from OPENAI_API_KEY, GEMINI_API_KEY, ANTHROPIC_API_KEY
#   - Port: 4000

evaluators:
  - name: quickstart
    type: judge
    phase: post_call
